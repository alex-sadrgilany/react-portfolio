import React, { useEffect } from "react";
import Navigation from "../Navigation";

function Header() {
    useEffect(() => {
        
    });

    return (
        <header>
            <h2>
                Alex
            </h2>
            <Navigation />
        </header>
    );
};

export default Header;